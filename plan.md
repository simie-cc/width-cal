## Plan: 純 HTML 寬度配置工具

建立一個不需建置工具或第三方套件、可直接由瀏覽器開啟的單頁工具。使用 HTML/CSS 呈現置中的總寬輸入與佔視窗可用寬度 90% 的 H 型量測線，JavaScript 以公分為唯一資料單位管理物件，再於渲染時換算成像素；物件可由單擊新增、雙擊修改寬度及水平拖曳，並即時標示物件寬度、各空白區段寬度與越界差異。

**Steps**
1. 建立靜態頁面骨架與可存取的控制項：頁面初始聚焦總寬數字輸入框，提供公分單位、確認方式與行內驗證訊息；尚未輸入有效正數時不顯示量測區，Enter 或失焦後建立／更新總寬。
2. 建立量測區視覺：以 SVG 或等效的精確定位容器繪製寬度為可用視窗 90% 的水平線，左右端各有垂直端點形成 H 型量測線；量測區保持固定高度並預留上下標籤空間，瀏覽器縮放或改變尺寸時只重新計算像素比例，不改變公分資料。
3. 在 JavaScript 建立單一狀態模型：保存 `totalWidthCm`、以唯一 ID 識別且依中心／左緣位置排序的 `objects`（位置與 `widthCm`）、目前編輯與拖曳狀態；所有幾何計算維持公分值，集中透過 `cmToPx`／`pxToCm` 換算顯示座標。
4. 實作純函式幾何計算：由已排序物件推導左端到第一物件、相鄰物件之間及最後物件到右端的空白區段；外側區段若為負值，改算為左／右越界量；集中判斷物件重疊、越界總量及顯示精度，避免事件處理器各自重複計算。
5. 實作單擊新增：將量測線上的點擊座標換算為公分，以該點為新物件中心並使用預設 40cm 寬度；允許物件超過 H 線左右邊界，越界時仍新增，但禁止與既有物件重疊。重疊時不新增並顯示簡短行內訊息；新增成功後重新排序與渲染。
6. 實作一致的渲染流程（*depends on 3-5*）：依狀態重建／更新物件矩形、物件寬度標籤與各空白區段標籤；正常物件使用中性色，任一端越界的物件改為紅色，對應端點另顯示「超出 X cm」，而線內各區段顯示其目前正值寬度。標籤採合理小數格式並避免超窄區段造成文字互相遮蓋（必要時移到交錯列或隱藏重複視覺文字但保留 title/ARIA 資訊）。
7. 實作雙擊寬度編輯：在物件附近顯示數字輸入框並帶入目前寬度；Enter 或失焦套用、Escape 取消。僅接受大於 0 的數值；以物件中心不變為原則更新兩側，允許因此越界並標紅，但若新寬度會與其他物件重疊則拒絕並保留原值與錯誤提示。
8. 實作水平拖曳：以 Pointer Events 或滑鼠事件記錄起點與原始位置，移動時將像素差換算為公分並即時渲染；拖曳可超過總寬端點以觸發紅色越界提示，但需限制到不與左右相鄰物件重疊。結束拖曳後保留公分位置，並抑制同一次拖曳所產生的 click，避免誤新增物件。
9. 補齊互動與版面細節：物件顯示可拖曳游標與選取狀態、錯誤訊息使用 `aria-live`、輸入與編輯欄有清楚 focus 樣式；支援桌機常見視窗寬度，狹窄畫面可水平容納標籤但本次不加入專屬觸控編輯流程。
10. 撰寫簡短使用說明於頁面必要位置或 README（若頁面可由控制項與提示自行理解則不增加說明區），列明直接開啟 `index.html` 即可使用；不加入套件管理器、伺服器、資料持久化、刪除、復原／重做或物件重疊功能。

**Relevant files**
- `/home/simie/work-wsl/width-cal/index.html` — 建立總寬輸入、量測畫布、編輯器／狀態訊息等語意化 DOM 結構並載入樣式與腳本。
- `/home/simie/work-wsl/width-cal/styles.css` — 定義置中初始畫面、90% 寬量測區、H 型線、物件、區段／越界標籤、錯誤及 focus/drag 狀態。
- `/home/simie/work-wsl/width-cal/app.js` — 實作狀態、單位換算、幾何推導、重疊與越界驗證、集中渲染，以及 click/dblclick/drag/resize 事件。
- `/home/simie/work-wsl/width-cal/README.md` — 可選；僅在需要補充直接開啟方式與操作摘要時建立。

**Verification**
1. 靜態檢查：以 `node --check app.js` 驗證 JavaScript 語法（若環境有 Node），並確認直接開啟 `index.html` 無 Console 錯誤、無網路或建置依賴。
2. 初始／縮放：開頁時焦點位於總寬輸入；無效值（空白、0、負數、非數字）顯示錯誤且不建立量測線；輸入 200 後 H 型線為畫面可用寬度 90%，改變瀏覽器寬度後線與物件等比重繪但公分標籤不變。
3. 新增：在 200cm 線中央單擊，建立中心位於 100cm、寬 40cm 的物件；在線內其他不重疊位置可繼續新增，各端與物件間區段寬度加總加上物件寬度應等於 200cm。
4. 越界：靠近左端或右端點擊時仍建立 40cm 物件；物件呈紅色並正確顯示單側超出量。測試物件寬於總寬時左右越界量與物件位置仍正確。
5. 衝突：在會與既有物件重疊的位置單擊不新增並顯示訊息；修改寬度或拖曳造成碰撞時停在相鄰物件邊界或拒絕變更，狀態中不得出現重疊。
6. 編輯：雙擊物件後輸入有效寬度並用 Enter／失焦套用；Escape 還原；0、負數、非數字與造成重疊的寬度不套用。擴大至越界時正確切換紅色並更新差異量。
7. 拖曳：左右拖曳時位置、兩側空白區段與越界量即時更新；快速拖曳、拖出線外及貼近相鄰物件時不穿越、不重疊，放開滑鼠不額外新增物件。
8. 視覺檢查：至少以 1440px、1024px 與窄桌面視窗檢查輸入、物件、寬度標籤和錯誤訊息無不合理重疊；確認鍵盤焦點及紅色異常狀態不只依賴顏色（同時有文字）。

**Decisions**
- 物件佔用輸入的固定總寬；「每個區段」包含每個物件本身，以及左右端和相鄰物件間的空白距離。
- 新物件固定以點擊位置為中心，預設 40cm；總寬不足不縮小物件，而是允許超出 H 線並以紅色顯示物件與超出公分數。
- 物件彼此不可重疊；新增／編輯若造成重疊則拒絕，拖曳時則夾限於相鄰物件邊界。
- 總寬只需為大於 0 的有效數值，不強制至少 40cm，因為需求明確允許預設物件越界。
- 本次為桌機優先：雙擊開啟行內數字輸入，Enter／失焦套用、Escape 取消；不額外設計觸控操作。
- 公分值是資料來源，90% 畫面寬只負責顯示比例，因此視窗尺寸變動不改變任何實際寬度或位置。
- 不在本次範圍內：刪除物件、物件重疊、資料儲存、匯入匯出、復原／重做、框架與建置流程。